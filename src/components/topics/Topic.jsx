/**
 * We are not using this component at the moment.
 * @param {*} param0
 * @returns
 */

const Topic = ({ description, name }) => {
  return (
    <section>
      <h2> {name} </h2>
      <p> {description} </p>
    </section>
  );
};

export default Topic;
